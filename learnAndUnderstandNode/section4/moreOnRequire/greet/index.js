const english = require('./english')
const spanish = require('./spanish')


// When the key and value have the same name you don't have to specify both values, just once.
module.exports = {
    english,
    spanish
}